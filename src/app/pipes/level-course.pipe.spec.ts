import { LevelCoursePipe } from './level-course.pipe';

describe('LevelCoursePipe', () => {
  it('create an instance', () => {
    const pipe = new LevelCoursePipe();
    expect(pipe).toBeTruthy();
  });
});
